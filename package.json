{
  "name": "thousand",
  "version": "1.0.0",
  "description": "Card Game 1000 — Lobby & Game Creation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage tests/*.test.js",
    "dev": "node --run start -- --watch"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
